{
  "name": "faker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Add dev script here'",
    "build": "echo 'Add build script here'",
    "test": "echo 'Add test script here'",
    "lint": "echo 'Add lint script here'",
	"test": "ts-node-dev --respawn --transpile-only src/seed.ts"
  },
  "dependencies": {
    "backend": "*",
    "ts-node-dev": "^2.0.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.4.1"
  }
}
